<h1>Here are all your posts:</h1>
<%= @posts.each do |post|%>
  <div class="card card-body bg-light mb-4 mt-3" style="max-width: 20rem">
    <h3><%= post.title %></h3>
    <p><%= post.body %></p>
    <%= link_to "Read More", post_path(post), :class => 'btn btn-primary' %>
  </div>
<% end %>